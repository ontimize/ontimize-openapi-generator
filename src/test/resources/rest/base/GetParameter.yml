components:
  schemas:
    FilterParam:
      in: query
      name: filter
      description: Filter
      schema:
        type: string
      required: false
    ColumnsParam:
      in: query
      name: columns
      description: Columns
      schema:
        type: string
      required: false
